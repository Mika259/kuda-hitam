# Kuda-hitam tool
education purposes 
Ddos HTTP(s) server for testing. Edu purpose

# How it's works?
Send HTTP GET request in loop. Web with big content will affected noticeable 

`git clone https://github.com/Mika259/kuda-hitam.git`

`cd kuda-hitam`

`python kuda.py`
